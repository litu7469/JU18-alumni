[build]
builder = "nixpacks"
buildCommand = "pip install -r backend/requirements.txt"

[deploy]
startCommand = "sh -c 'cd /app/backend && alembic upgrade head && uvicorn main:app --host 0.0.0.0 --port $PORT'"
healthcheckPath = "/health"
